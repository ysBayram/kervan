node_id: "kervan-node-0"
listeners:
  - bind: ":8080"
    protocol: "tcp"
    max_sessions: 10000
  - bind: ":8081"
    protocol: "websocket"
    max_sessions: 10000
upstream:
  addr: "backend:9000"
session:
  shard_count: 256
  idle_timeout: 15m
  read_buffer_size: 4096
metrics_addr: ":9090"
